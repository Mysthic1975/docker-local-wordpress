Im Verzeichnis CMD / PS ausführen und folgenden Befehl eingeben: 'docker compose up' ohne (''), 
Docker muss laufen im Hintergrund und warten bis es fertig ist.
Dann localhost:8080 eingeben und einrichten / anpassen.
